spring:
  application:
    name: brand-engine

  datasource:
    url: jdbc:postgresql://localhost:5433/brand_engine
    username: postgres
    password: qwe12345

  jpa:
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      embedding:
        options:
          model: text-embedding-3-small

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  storage:
    location: ${FILE_STORAGE_LOCATION:uploads}

  jwt:
    secret: ${JWT_SECRET:VGh5bmthaC1kZXZlbG9wbWVudC1qd3Qtc2VjcmV0LWtleS0yMDI2LXBsZWFzZS1yZXBsYWNl}
    expiration-ms: 86400000

ai:
  provider: openai

  openai:
    api-key: ${OPENAI_API_KEY}
    model: gpt-5.6