CREATE TABLE conversation (

                              id UUID PRIMARY KEY,

                              title VARCHAR(255) NOT NULL,

                              created_at TIMESTAMP NOT NULL

);